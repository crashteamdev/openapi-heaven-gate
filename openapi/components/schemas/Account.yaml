type: object
properties:
  email:
    type: string
  blocked:
    type: boolean
    default: false
  subscription:
    type: array
    items:
      type: object
      properties:
        product:
          $ref: ./Product.yaml
        subscription:
          $ref: ./Subscription.yaml
