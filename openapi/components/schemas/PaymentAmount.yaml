type: object
properties:
  value:
    type: integer
    format: int64
    description: payment amount value (minor units)
  currencyCode:
    type: integer
    description: payment currency code (ISO-4217 format)
